node_modules/
.DS_Store
.env
Workflows/**/dist/
temp/
# Dumps de corrida para diagnóstico (scrape crudo, MB, con tokens de CDN efímeros): no van a git
outputs/
outputs-main/
outputs-referentes/

# Snapshots del live de n8n antes de cada push (ADR-053) — contienen las keys resueltas
.n8n-snapshots/
launch.json
